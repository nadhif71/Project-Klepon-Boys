version: '3.8'
services:
  db:
    image: postgres:15-alpine
    container_name: infoinfo
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: otintern
    ports:
      - "5432:5432"
    volumes:
      - ./init:/docker-entrypoint-initdb.d